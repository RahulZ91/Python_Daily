A parser is a small program that takes a text file as an input, breaks it into tokens and organises the token as per rules.
The project aims to develop a parser that input the text file and create the tokens
